import styled from 'styled-components';

export const StyledIndex = styled.div`
  display: grid;
  grid-template-columns: repeat(auto, minmax(1fr, 2fr));
  grid-template-rows: auto 1fr;
  column-gap: 2rem;
`;
